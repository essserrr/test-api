allOf:
  - $ref: ./Response-Paginated.yaml
  - type: object
    description: Category list response
    properties:
      data:
        properties:
          data:
            type: array
            items:
              $ref: ../schemas/Category.yaml
