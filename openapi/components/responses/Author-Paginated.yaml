allOf:
  - $ref: ./Response-Paginated.yaml
  - type: object
    description: Author list response
    properties:
      data:
        properties:
          data:
            type: array
            items:
              $ref: ../schemas/Author.yaml
