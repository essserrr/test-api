allOf:
  - $ref: ./Response-Paginated.yaml
  - type: object
    description: Draft list response
    properties:
      data:
        properties:
          data:
            type: array
            items:
              $ref: ../schemas/News.yaml
